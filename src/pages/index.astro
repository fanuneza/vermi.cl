---
// index.astro - vermi.cl Homepage
import BaseLayout from '../layouts/BaseLayout.astro';
import IsItCompostable from '../components/IsItCompostable.astro';
import BlogCardGrid from '../components/BlogCardGrid.astro';
import CategoryCard from '../components/CategoryCard.astro';
import { getLatestPosts } from '../utils/blog';
import { faWorm, faCarrot, faBookOpen, faAppleWhole, faSkullCrossbones } from '@fortawesome/free-solid-svg-icons';
import { Image, getImage } from 'astro:assets';
import heroImage from '../assets/images/hero.jpg';

// Retrieve latest 3 posts for the home feed
const sortedPosts = await getLatestPosts(3);

const wormPaths = Array.isArray(faWorm.icon[4]) ? faWorm.icon[4] : [faWorm.icon[4]];
const carrotPaths = Array.isArray(faCarrot.icon[4]) ? faCarrot.icon[4] : [faCarrot.icon[4]];
const bookOpenPaths = Array.isArray(faBookOpen.icon[4]) ? faBookOpen.icon[4] : [faBookOpen.icon[4]];
const appleWholePaths = Array.isArray(faAppleWhole.icon[4]) ? faAppleWhole.icon[4] : [faAppleWhole.icon[4]];
const skullCrossbonesPaths = Array.isArray(faSkullCrossbones.icon[4]) ? faSkullCrossbones.icon[4] : [faSkullCrossbones.icon[4]];

import { buildSchemaGraph } from '../utils/schema';
import { HOME_METADATA } from '../utils/site-metadata';

const homepageSchema = buildSchemaGraph({
  pageType: 'website',
  url: 'https://vermi.cl/',
  title: 'Inicio',
  description: HOME_METADATA.description,
});


// Optimize LCP hero image for responsive preloading
const optimizedHero = await getImage({
  src: heroImage,
  width: 600,
  height: 450,
  format: 'webp',
  widths: [320, 480, 600],
  quality: 'mid'
});

const heroSrcSet = optimizedHero.srcSet.values
  .map(item => `${item.url} ${item.descriptor}`)
  .join(', ');
---

<BaseLayout title={HOME_METADATA.title} description={HOME_METADATA.description} schema={homepageSchema}>
  <link 
    rel="preload" 
    as="image" 
    href={optimizedHero.src} 
    imagesrcset={heroSrcSet} 
    imagesizes="(max-width: 480px) 320px, (max-width: 1024px) 480px, 600px"
    fetchpriority="high" 
    slot="head"
  />
  <!-- Hero Section -->
  <section class="w-full max-w-7xl grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12 items-center relative z-10">
    <!-- Decorative Background Element -->
    <div class="absolute -top-12 -left-12 w-64 h-64 bg-secondary-fixed rounded-full mix-blend-multiply opacity-60 blur-2xl z-[-1]"></div>
    
    <div class="lg:col-span-6 flex flex-col items-start gap-6 relative z-10">
      <!-- Badge -->
      <div class="bg-tertiary-fixed text-on-tertiary-fixed font-label-md text-label-md px-3 py-1 border-3 border-on-surface shadow-hard-sm rotate-[-3deg] uppercase tracking-wider badge--sticker inline-block">
        Aprende hoy
      </div>
      
      <h1 class="font-headline text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-black text-on-surface leading-tight sm:leading-none relative uppercase m-0">
        Comienza a vermicompostar tus residuos en casa
        <svg class="absolute -bottom-4 left-0 w-full h-4 text-primary" preserveAspectRatio="none" viewBox="0 0 100 10">
          <path d="M0,5 Q50,15 100,5" fill="none" stroke="currentColor" stroke-width="4"></path>
        </svg>
      </h1>
      
      <p class="font-body text-lg text-on-surface-variant max-w-md mt-2 leading-relaxed">
        Aprende a reciclar tus residuos de cocina con lombrices californianas (<span class="italic font-bold">Eisenia fetida</span>). Transforma la materia orgánica en humus de alta calidad agrícola y dile adiós a la basura en tu hogar chileno.
      </p>
      
      <div class="flex flex-wrap gap-4 mt-4">
        <a href="/blog/" class="bg-primary text-on-primary font-headline text-lg px-6 py-3 rounded-lg btn--brutal flex items-center gap-2 no-underline">
          Explora nuestro blog
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
        </a>
        <a href="#buscador-alimentos" class="bg-secondary-container text-on-secondary-container font-headline text-lg px-6 py-3 rounded-lg btn--brutal flex items-center gap-2 rotate-1 no-underline">
          ¿Qué pueden comer? 🪱
        </a>
      </div>
    </div>
    
    <div class="lg:col-span-6 relative mt-8 lg:mt-0 order-first lg:order-none">
      <div class="relative w-full aspect-video md:aspect-square lg:aspect-[4/3] z-10">
        <Image 
          class="w-full h-full object-cover organic-shape-1 border-4 border-on-surface hard-shadow bg-tertiary-fixed" 
          src={heroImage} 
          alt="Compostaje de lombrices californianas" 
          width={600} 
          height={450} 
          widths={[320, 480, 600]}
          sizes="(max-width: 480px) 320px, (max-width: 1024px) 480px, 600px"
          loading="eager" 
          fetchpriority="high" 
          quality="mid"
        />
        <!-- Decorative Floating Elements -->
        <div class="absolute -bottom-2 -right-2 bg-surface-dim p-4 border-4 border-on-surface organic-shape-2 shadow-hard-sm rotate-[15deg]">
          <svg xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox={`0 0 ${faCarrot.icon[0]} ${faCarrot.icon[1]}`} fill="currentColor" class="text-primary">
            {carrotPaths.map((d) => <path d={d} />)}
          </svg>
        </div>
        <div class="absolute -top-2 -left-2 bg-error-container p-3 border-2 border-on-surface rounded-full shadow-[2px_2px_0px_0px_#002201] rotate-[-10deg]">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox={`0 0 ${faWorm.icon[0]} ${faWorm.icon[1]}`} fill="currentColor" class="text-on-error-container">
            {wormPaths.map((d) => <path d={d} />)}
          </svg>
        </div>
      </div>
    </div>
  </section>

  <!-- Interactive Food Search Widget -->
  <IsItCompostable />

  <!-- Category Cards Grid -->
  <section class="w-full max-w-7xl relative z-10 flex flex-col gap-8">
    <h2 class="font-headline text-3xl md:text-4xl font-black text-on-surface self-start relative inline-block uppercase">
      Aprende con nosotros
      <div class="absolute -bottom-2 left-0 w-full h-2 bg-secondary opacity-50 -rotate-1"></div>
    </h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <!-- Card 1 -->
      <CategoryCard
        href="/blog/?category=Manual"
        title="Manual de inicio"
        description="Aprende a configurar la vermicompostera ideal para tu núcleo de cría utilizando desecho local y adáptala a la inercia térmica de tu balcón."
        badgeText="BASICO"
        badgeBgClass="bg-surface-dim"
        cardBgClass="bg-tertiary-fixed"
        organicShapeClass="organic-shape-2"
        rotationClass="rotate-1"
        iconBoxRotationClass="-rotate-3"
        textColorHoverClass="group-hover:text-primary"
      >
        <svg slot="icon" xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox={`0 0 ${faBookOpen.icon[0]} ${faBookOpen.icon[1]}`} fill="currentColor" class="text-primary">
          {bookOpenPaths.map((d) => <path d={d} />)}
        </svg>
      </CategoryCard>
      
      <!-- Card 2 -->
      <CategoryCard
        href="/blog/?category=Alimentación"
        title="Alimentación"
        description="El menú ideal para tus lombrices. Descubre el balance óptimo de nitrógeno y carbono y el truco de la congelación celular."
        badgeText="DIARIO"
        badgeBgClass="bg-secondary-fixed-dim"
        cardBgClass="bg-surface-container"
        organicShapeClass="organic-shape-1"
        rotationClass="-rotate-1"
        iconBoxRotationClass="rotate-2"
        textColorHoverClass="group-hover:text-secondary"
      >
        <svg slot="icon" xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox={`0 0 ${faAppleWhole.icon[0]} ${faAppleWhole.icon[1]}`} fill="currentColor" class="text-secondary">
          {appleWholePaths.map((d) => <path d={d} />)}
        </svg>
      </CategoryCard>
      
      <!-- Card 3 -->
      <CategoryCard
        href="/blog/?category=Problemas"
        title="Solución de problemas"
        description="¿Mosquitos, acidez o putrefacción anaeróbica? Evita la fuga nocturna de tu compostera y mantén el ecosistema estable y sin olor."
        badgeText="URGENTE"
        badgeBgClass="bg-error-container"
        cardBgClass="bg-inverse-primary"
        organicShapeClass="organic-shape-3"
        rotationClass="rotate-2"
        iconBoxRotationClass="-rotate-2"
        textColorHoverClass="group-hover:text-tertiary"
        extraClasses="lg:col-span-1 md:col-span-2"
      >
        <svg slot="icon" xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox={`0 0 ${faSkullCrossbones.icon[0]} ${faSkullCrossbones.icon[1]}`} fill="currentColor" class="text-tertiary">
          {skullCrossbonesPaths.map((d) => <path d={d} />)}
        </svg>
      </CategoryCard>
    </div>
  </section>

  <!-- Latest Guides Section -->
  <section class="w-full max-w-7xl relative z-10 flex flex-col gap-8">
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
      <h2 class="font-headline text-3xl md:text-4xl font-black text-on-surface relative inline-block uppercase">
        Últimas guías publicadas
        <div class="absolute -bottom-2 left-0 w-full h-2 bg-secondary opacity-50 -rotate-1"></div>
      </h2>
      <a href="/blog/" class="bg-white text-on-background font-label-md text-label-md px-4 py-2 border-2 border-on-surface shadow-hard-sm hover:translate-x-[4px] hover:translate-y-[4px] hover:shadow-none transition-all active:scale-95 organic-brutalism no-underline">
        Ver todas las guías
      </a>
    </div>
    
    <BlogCardGrid posts={sortedPosts} />
  </section>
</BaseLayout>
